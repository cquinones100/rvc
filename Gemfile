source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

ruby '2.6.0'
gem 'sinatra'
gem 'rack'

# Specify your gem's dependencies in rvc_compiler.gemspec
gemspec
